{
  "build": { "installCommand": "pip install -r requirements.txt && npm install" },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level debug",
    "healthcheckPath": "/ping"
  }
}
